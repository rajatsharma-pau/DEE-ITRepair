<!DOCTYPE html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>DEE Employee & IT Repair Management System</title><link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"><style>.navbar{background:#198754}.navbar a{color:#fff}.card-header{background:#e9f7ef;font-weight:bold}.badge-status{text-transform:capitalize}</style></head><body>
<nav class="navbar navbar-expand-lg"><a class="navbar-brand text-white" href="{{ route('dee.dashboard') }}">DEE IT Repair System</a><div class="ml-auto">@auth('dee')<span class="text-white mr-3">{{ auth('dee')->user()->name }} ({{ auth('dee')->user()->role }})</span><a class="btn btn-sm btn-light" href="{{ route('dee.logout') }}" onclick="event.preventDefault();document.getElementById('logout-form').submit();">Logout</a><form id="logout-form" method="POST" action="{{ route('dee.logout') }}" style="display:none">@csrf</form>@endauth</div></nav>
<div class="container mt-4">@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif @if($errors->any())<div class="alert alert-danger"><ul class="mb-0">@foreach($errors->all() as $e)<li>{{ $e }}</li>@endforeach</ul></div>@endif @yield('content')</div>
<script src="https://code.jquery.com/jquery-3.5.1.min.js"></script><script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.bundle.min.js"></script>@yield('scripts')</body></html>
