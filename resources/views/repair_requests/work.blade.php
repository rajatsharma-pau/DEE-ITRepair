@extends('layouts.app') @section('content')<h3>Programmer Update {{ $repair_request->request_no }}</h3><form method="POST" action="{{ route('repair-requests.work',$repair_request) }}">@csrf <div class="form-group"><label>Status</label><select name="status" class="form-control">@foreach(['in_progress','need_part','sent_outside','repaired','not_repairable'] as $s)<option value="{{ $s }}">{{ $s }}</option>@endforeach</select></div><div class="form-group"><label>Programmer Remarks</label><textarea name="programmer_remarks" class="form-control" required></textarea></div><button class="btn btn-success">Save Update</button></form>@endsection
