@extends('layouts.app') @section('content')<h3>Forward Request {{ $repair_request->request_no }}</h3><form method="POST" action="{{ route('repair-requests.forward',$repair_request) }}">@csrf <div class="form-group"><label>Programmer</label><select name="programmer_id" class="form-control">@foreach($programmers as $p)<option value="{{ $p->id }}">{{ $p->name }} - {{ $p->phone }}</option>@endforeach</select></div><div class="form-group"><label>Storekeeper Verification Remarks</label><textarea name="storekeeper_remarks" class="form-control" required></textarea></div><button class="btn btn-success">Forward</button></form>@endsection
