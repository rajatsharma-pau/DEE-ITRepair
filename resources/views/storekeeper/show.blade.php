@extends('layouts.app')
@section('content')<h3>Verify {{ $repair_request->request_no }}</h3><p>{{ $repair_request->problem_description }}</p><form method="POST" action="{{ route('storekeeper.requests.forward',$repair_request) }}">@csrf <div class="form-group"><label>Forward to Programmer</label><select name="programmer_id" class="form-control" required>@foreach($programmers as $p)<option value="{{ $p->id }}">{{ $p->name }}</option>@endforeach</select></div><div class="form-group"><label>Storekeeper Remarks</label><textarea name="storekeeper_remarks" class="form-control" required></textarea></div><button class="btn btn-success">Verify & Forward</button></form>@endsection
